<% provide(:title, @user.nom) %>

<h1>
  <%= @user.identifiant %> (<%= "#{@user.nom.upcase} #{@user.prenom}" %>)
  <% if @user.benev? %>
    <br>Bénévole
  <% else %>
    <br>Salarié
  <% end %>
  <% if @user.admin? %>
    <br>Administrateur
  <% end %>

  <% if !current_user.admin? %>
    <% if current_user?(@user) %>
      <br><%= link_to "Editer", edit_user_path(@user) %>
    <% end %>

  <% else %>
    <br><%= link_to "Editer", edit_user_path(@user) %>
    <% if !current_user?(@user) %>
      | <%= link_to "Supprimer", @user, method: :delete,
                                     data: { confirm: "Etes-vous sûr(e) ?" } %>
    <% end %>
  <% end %>
</h1>