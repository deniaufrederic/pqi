<li class="lead">
  <%= "#{user.nom} #{user.prenom}" %>
  <% if user.benev? %>
    <br>Bénévole
  <% else %>
    <br>Salarié
  <% end %>
  <% if user.admin? %>
  	<br>Administrateur
  <% end %>
  <br><%= link_to user.identifiant, user %>
</li>