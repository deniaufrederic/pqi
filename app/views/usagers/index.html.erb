<% provide(:title, 'Usagers') %>
<h1>Usagers</h1>

<%= will_paginate %>

  <%= form_tag(usagers_path, :method => "get", id: "search-form") do %>
    <%= text_field_tag :search, params[:search], placeholder: "Rechercher des usagers" %>
    <%= submit_tag "Rechercher", class: 'btn btn-primary' %>
  <% end %>

  <%= link_to "Recherche avancée", new_search_path %>

<table class="usagers table table-hover">
  <tr id="table_head">
    <%= render 'table_h_soft' %>
    <th class="supp">Edition et Suppression (privilège admin)</th>
  </tr>
  <% if !@usagers.empty? %>
    <%= render @usagers %>
  <% else %>
    <p>Aucun usager correspondant au(x) terme(s) : "<%= params[:search] %>".</p>
  <% end %>
</table>

<%= will_paginate %>